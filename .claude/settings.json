{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "if [ -n \"$(git diff --name-only HEAD -- backend)\" ] && [ -z \"$(git diff --name-only HEAD -- README.md)\" ]; then echo '{\"systemMessage\": \"backend/ changed but README.md did not this session \\u2014 check for doc drift before wrapping up.\"}'; fi",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
